@model VisioGeneral.Web.Models.Entities.Programa
@{
    ViewData["Title"] = "Eliminar programa";
}

<div class="mb-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">Programes</a></li>
            <li class="breadcrumb-item active">Eliminar</li>
        </ol>
    </nav>
    <h1 class="section-title text-danger">Eliminar programa</h1>
</div>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        @TempData["Error"]
    </div>
}

<div class="row">
    <div class="col-md-6">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <i class="bi bi-exclamation-triangle"></i> Confirmar eliminació
            </div>
            <div class="card-body">
                <h5 class="card-title">@Model.Nom</h5>
                <dl class="row mb-0">
                    <dt class="col-sm-4">Àrea</dt>
                    <dd class="col-sm-8">
                        <span class="badge" style="background-color: @Model.Servei.Area.Color; color: white;">@Model.Servei.Area.Codi</span>
                        @Model.Servei.Area.Nom
                    </dd>
                    <dt class="col-sm-4">Servei</dt>
                    <dd class="col-sm-8">@Model.Servei.Nom</dd>
                    @if (Model.Director != null)
                    {
                        <dt class="col-sm-4">Director/a</dt>
                        <dd class="col-sm-8">@Model.Director.NomComplet</dd>
                    }
                    <dt class="col-sm-4">Treballadors</dt>
                    <dd class="col-sm-8">@Model.NumTreballadors</dd>
                </dl>

                @if (ViewBag.TeDependencies)
                {
                    <div class="alert alert-warning mt-3 mb-0">
                        <i class="bi bi-exclamation-triangle"></i> <strong>Atenció:</strong> Aquest programa no es pot eliminar perquè té:
                        <ul class="mb-0 mt-2">
                            <li>@ViewBag.NumQuestions qüestió(ns) associada(es)</li>
                        </ul>
                        <p class="mt-2 mb-0">Primer elimina o reassigna les qüestions.</p>
                    </div>
                }
            </div>
            <div class="card-footer">
                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex gap-2">
                        @if (!ViewBag.TeDependencies)
                        {
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Sí, eliminar
                            </button>
                        }
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            @(ViewBag.TeDependencies ? "Tornar" : "Cancel·lar")
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
